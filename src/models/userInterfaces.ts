export interface ILoginViewModel {
    email: string;
    password: string;
}

export interface IRegistrationViewModel {
    email: string;
    name: string;
    surname: string;
    password: string;
    phoneNumber: string;
    placeStudy: string;
    placeWork: string;
}